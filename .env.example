DEEPL_AUTH_KEY=
# Alternative: DEEPL_AUTH_KEY_OP_REF=op://<vault>/<item>/<field>
# A 1Password CLI (op) reference, not a secret itself. Resolved lazily on first
# use via `op read`. Set only one of DEEPL_AUTH_KEY / DEEPL_AUTH_KEY_OP_REF.
